# chatlas
Проект «Telegram-бот для получения списка участников чата по истории сообщений» по дисциплине "Проектная практика" (хакатон). 1-й семестр 2-го курса МИФИ ИИКС РПО (2025-2026 уч. г).

## Описание проекта

Проект представляет собой Telegram бота, предназначенного для получения списка участников Telegram чата по отправляемому стандартному файлу выгрузки экспорта.

- С описанием хакатона можно подробно ознакомиться в файле [theory.md](/documentation/theory.md).
- С описанием самого задания можно подробно ознакомиться в файле [task.md](/documentation/task.md).

## Стек проекта

- Java 21.
- Maven.
  - TelegramBots (longpolling + client).
  - Jackson (core + datatype).
  - Apache POI (XLSX).
  - SLF4J.
  - JUnit 5.
  - Lombok.
- Docker.

## Запуск бота

Чтобы запустить проект, необходимо иметь запущенный Docker-демон.

Перед запуском необходимо:

1. Получить токен бота у `@BotFather`.
2. Создать в корне репозитория `.env` файл с содержимым `TELEGRAM_BOT_TOKEN=REAL_TOKEN`, где `REAL_TOKEN` - токен, полученный от `@BotFather`.
3. Запустить проект командой `docker compose up --build`.